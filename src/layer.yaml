includes:
 - layer:basic
 - layer:snap
 - interface:nrpe-external-master
 - interface:pgsql
 - interface:mysql-shared
 - interface:etcd
options:
  basic:
    packages:
      - python3-psycopg2
  snap:
    vault:
      channel: stable
resources:
  vault:
    type: file
    filename: vault.snap
    description: Vault snap
